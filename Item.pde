class Item extends BaseCharacter {

  public Item( Game parent,String spriteName, int cols, int rows ) {
    super( parent, spriteName, cols, rows );
  }
  
  public void update( double deltaTime ) {
    if ( mousePressed ) { 
      
    }
    super.update( deltaTime );
  }
}

